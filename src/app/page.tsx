import ProfileReadme from "@/features/profile/components/ProfileReadme";

// Title and description come from the root layout's metadata defaults.
const HomePage = () => {
  return <ProfileReadme />;
};

export default HomePage;

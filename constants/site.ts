// The single canonical origin for SEO. This site is deployed to two hosts —
// GitHub Pages (ipondnakab.github.io) and Vercel (kittipat.dev) — but ships the
// same static build to both. Every canonical tag, sitemap entry, OpenGraph URL
// and structured-data URL points here on BOTH deploys, so search engines
// consolidate ranking onto one domain instead of treating the two as duplicate
// content. kittipat.dev is the primary/canonical domain.
export const SITE_URL = "https://kittipat.dev";

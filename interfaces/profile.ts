// One selectable picture in the home-page avatar. `alt` describes the artwork
// itself, so it stays untranslated — it is not UI copy.
export interface ProfileAvatarImage {
  src: string;
  alt: string;
}
